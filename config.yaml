# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: ens-indexer
description: ENS Protocol Indexer

contracts:
  # ─── Registry Contracts ──────────────────────────────────────────────────────
  - name: RegistryOld
    events:
      - event: "NewOwner(bytes32 indexed node, bytes32 indexed label, address owner)"
        field_selection:
          transaction_fields:
            - hash
      - event: "Transfer(bytes32 indexed node, address owner)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NewResolver(bytes32 indexed node, address resolver)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NewTTL(bytes32 indexed node, uint64 ttl)"
        field_selection:
          transaction_fields:
            - hash

  - name: Registry
    events:
      - event: "NewOwner(bytes32 indexed node, bytes32 indexed label, address owner)"
        field_selection:
          transaction_fields:
            - hash
      - event: "Transfer(bytes32 indexed node, address owner)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NewResolver(bytes32 indexed node, address resolver)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NewTTL(bytes32 indexed node, uint64 ttl)"
        field_selection:
          transaction_fields:
            - hash

  # ─── BaseRegistrar ──────────────────────────────────────────────────────────
  - name: BaseRegistrar
    events:
      - event: "NameRegistered(uint256 indexed id, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(uint256 indexed id, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "Transfer(address indexed from, address indexed to, uint256 indexed tokenId)"
        field_selection:
          transaction_fields:
            - hash

  # ─── NameWrapper ─────────────────────────────────────────────────────────────
  - name: NameWrapper
    events:
      - event: "NameWrapped(bytes32 indexed node, bytes name, address owner, uint32 fuses, uint64 expiry)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameUnwrapped(bytes32 indexed node, address owner)"
        field_selection:
          transaction_fields:
            - hash
      - event: "FusesSet(bytes32 indexed node, uint32 fuses)"
        field_selection:
          transaction_fields:
            - hash
      - event: "ExpiryExtended(bytes32 indexed node, uint64 expiry)"
        field_selection:
          transaction_fields:
            - hash
      - event: "TransferSingle(address indexed operator, address indexed from, address indexed to, uint256 id, uint256 value)"
        field_selection:
          transaction_fields:
            - hash
      - event: "TransferBatch(address indexed operator, address indexed from, address indexed to, uint256[] ids, uint256[] values)"
        field_selection:
          transaction_fields:
            - hash

  # ─── Registrar Controllers ──────────────────────────────────────────────────
  - name: LegacyController
    events:
      - event: "NameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 cost, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(string name, bytes32 indexed label, uint256 cost, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash

  - name: WrappedController
    events:
      - event: "NameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 baseCost, uint256 premium, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(string name, bytes32 indexed label, uint256 cost, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash

  - name: UnwrappedController
    events:
      - event: "NameRegistered(string label, bytes32 indexed labelhash, address indexed owner, uint256 baseCost, uint256 premium, uint256 expires, bytes32 referrer)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(string label, bytes32 indexed labelhash, uint256 cost, uint256 expires, bytes32 referrer)"
        field_selection:
          transaction_fields:
            - hash

  # ─── Base L2 Contracts ─────────────────────────────────────────────────────
  - name: BaseRegistrar_Base
    events:
      - event: "NameRegistered(uint256 indexed id, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRegisteredWithRecord(uint256 indexed id, address indexed owner, uint256 expires, address resolver, uint64 ttl)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(uint256 indexed id, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "Transfer(address indexed from, address indexed to, uint256 indexed tokenId)"
        field_selection:
          transaction_fields:
            - hash

  - name: EAController_Base
    events:
      - event: "NameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash

  - name: RegController_Base
    events:
      - event: "NameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(string name, bytes32 indexed label, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash

  - name: UpgController_Base
    events:
      - event: "NameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(string name, bytes32 indexed label, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash

  # ─── Linea L2 Contracts ────────────────────────────────────────────────────
  - name: BaseRegistrar_Linea
    events:
      - event: "NameRegistered(uint256 indexed id, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(uint256 indexed id, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "Transfer(address indexed from, address indexed to, uint256 indexed tokenId)"
        field_selection:
          transaction_fields:
            - hash

  - name: EthController_Linea
    events:
      - event: "NameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 baseCost, uint256 premium, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameRenewed(string name, bytes32 indexed label, uint256 cost, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "OwnerNameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash
      - event: "PohNameRegistered(string name, bytes32 indexed label, address indexed owner, uint256 expires)"
        field_selection:
          transaction_fields:
            - hash

  # ─── Resolver (dynamically registered via Registry.NewResolver) ─────────────
  - name: Resolver
    events:
      - event: "AddrChanged(bytes32 indexed node, address a)"
        field_selection:
          transaction_fields:
            - hash
      - event: "AddressChanged(bytes32 indexed node, uint256 coinType, bytes newAddress)"
        field_selection:
          transaction_fields:
            - hash
      - event: "NameChanged(bytes32 indexed node, string name)"
        field_selection:
          transaction_fields:
            - hash
      - event: "ABIChanged(bytes32 indexed node, uint256 indexed contentType)"
        field_selection:
          transaction_fields:
            - hash
      - event: "PubkeyChanged(bytes32 indexed node, bytes32 x, bytes32 y)"
        field_selection:
          transaction_fields:
            - hash
      - event: "TextChanged(bytes32 indexed node, string indexed indexedKey, string key, string value)"
        field_selection:
          transaction_fields:
            - hash
      - event: "ContenthashChanged(bytes32 indexed node, bytes hash)"
        field_selection:
          transaction_fields:
            - hash
      - event: "InterfaceChanged(bytes32 indexed node, bytes4 indexed interfaceID, address implementer)"
        field_selection:
          transaction_fields:
            - hash
      - event: "AuthorisationChanged(bytes32 indexed node, address indexed owner, address indexed target, bool isAuthorised)"
        field_selection:
          transaction_fields:
            - hash
      - event: "VersionChanged(bytes32 indexed node, uint64 newVersion)"
        field_selection:
          transaction_fields:
            - hash

# ─── Chain Configuration ───────────────────────────────────────────────────────
chains:
  - id: 1 # Ethereum Mainnet
    start_block: 3327417
    contracts:
      - name: RegistryOld
        address:
          - "0x314159265dd8dbb310642f98f50c066173c1259b"
      - name: Registry
        address:
          - "0x00000000000c2e074ec69a0dfb2997ba6c7d2e1e"
        start_block: 9380380
      - name: BaseRegistrar
        address:
          - "0x57f1887a8bf19b14fc0df6fd9b2acc9af147ea85"
        start_block: 9380410
      - name: NameWrapper
        address:
          - "0xd4416b13d2b3a9abae7acd5d6c2bbdbe25686401"
        start_block: 16925608
      - name: LegacyController
        address:
          - "0x283af0b28c62c092c9727f1ee09c02ca627eb7f5"
        start_block: 9380471
      - name: WrappedController
        address:
          - "0x253553366da8546fc250f225fe3d25d0c782303b"
        start_block: 16925618
      - name: UnwrappedController
        address:
          - "0x59e16fccd424cc24e280be16e11bcd56fb0ce547"
        start_block: 22764821
      - name: Resolver
        # Dynamically registered via Registry.NewResolver contractRegister
  - id: 8453 # Base
    start_block: 17571480
    contracts:
      - name: Registry
        address:
          - "0xb94704422c2a1e396835a571837aa5ae53285a95"
      - name: BaseRegistrar_Base
        address:
          - "0x03c4738ee98ae44591e1a4a4f3cab6641d95dd9a"
        start_block: 17571486
      - name: EAController_Base
        address:
          - "0xd3e6775ed9b7dc12b205c8e608dc3767b9e5efda"
        start_block: 17575699
      - name: RegController_Base
        address:
          - "0x4ccb0bb02fcaba27e82a56646e81d8c5bc4119a5"
        start_block: 18619035
      - name: UpgController_Base
        address:
          - "0xa7d2607c6bd39ae9521e514026cbb078405ab322"
        start_block: 35286620
      - name: Resolver
        # Dynamically registered via Registry.NewResolver contractRegister
  - id: 59144 # Linea
    start_block: 6682888
    contracts:
      - name: Registry
        address:
          - "0x50130b669b28c339991d8676fa73cf122a121267"
      - name: BaseRegistrar_Linea
        address:
          - "0x6e84390dcc5195414ec91a8c56a5c91021b95704"
        start_block: 6682892
      - name: NameWrapper
        address:
          - "0xa53cca02f98d590819141aa85c891e2af713c223"
        start_block: 6682956
      - name: EthController_Linea
        address:
          - "0xdb75db974b1f2bd3b5916d503036208064d18295"
        start_block: 6682978
      - name: Resolver
        # Dynamically registered via Registry.NewResolver contractRegister

address_format: lowercase
